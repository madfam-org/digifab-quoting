apiVersion: v1
kind: Service
metadata:
  name: digifab-quoting-api
  namespace: digifab-quoting
spec:
  type: ClusterIP
  selector:
    app: digifab-quoting-api
  ports:
  - port: 80
    targetPort: 4000
    protocol: TCP
