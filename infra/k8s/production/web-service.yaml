apiVersion: v1
kind: Service
metadata:
  name: digifab-quoting-web
  namespace: digifab-quoting
spec:
  type: ClusterIP
  selector:
    app: digifab-quoting-web
  ports:
  - port: 80
    targetPort: 3000
    protocol: TCP
